# Backend REST API base URL (same backend used by apps/web and apps/api)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Backend WebSocket namespace base URL (socket.io)
NEXT_PUBLIC_WS_URL=http://localhost:5000
